import { PROTOCOL_CONSTANTS } from './constants';
/**
 * 生成header信息
 * @param message_type_specific_flags
 * @param message_type
 * @param version
 * @param serial_method
 * @param compression_type
 * @param reserved_data
 * @param extension_header
 * @returns
 */
export declare const generateHeader: (message_type_specific_flags?: PROTOCOL_CONSTANTS, message_type?: PROTOCOL_CONSTANTS, version?: PROTOCOL_CONSTANTS, serial_method?: PROTOCOL_CONSTANTS, compression_type?: PROTOCOL_CONSTANTS, reserved_data?: number, extension_header?: ArrayBuffer) => DataView;
export declare function convertBlobToArrayBuffer(blob: Blob): Promise<ArrayBuffer>;
/**
 * 创建一个full request client类型的事件包
 * @param event
 * @param option
 * @returns
 */
export declare const buildFullClientRequestBody: (event: number, option?: {
  session_id?: string;
  payload?: Record<string, any>;
  seq?: number;
}) => Blob;
export declare function getEventName(num: number): string;